/**
 * @typedef {Object} TableMeta
 * @property {string} tableName The raw table name, used verbatim as a runtime object key.
 * @property {string} singular The identifier-safe singular type name (namespaced by database).
 * @property {string} databaseName The raw database name, used verbatim as a runtime object key.
 */
export {};
